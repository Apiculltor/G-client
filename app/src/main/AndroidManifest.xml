<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- === PERMISSÕES BÁSICAS PARA VUZIX BLADE === -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <!-- === CONECTIVIDADE VUZIX === -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- === SISTEMA DE HÁBITOS === -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SET_ALARM" />
    
    <!-- === FEATURES DE HARDWARE === -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/HudTheme">
        
        <!-- === ACTIVITY PRINCIPAL === -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/HudTheme"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- === RECEIVERS PARA CONECTIVIDADE VUZIX === -->
        <receiver 
            android:name=".VuzixConnectivityReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.seudominio.vuzixbladeapp.ACTION_DATA_FROM_S24" />
                <action android:name="com.seudominio.vuzixbladeapp.ACTION_FEEDBACK_FROM_S24" />
            </intent-filter>
        </receiver>
        
        <!-- === RECEIVERS PARA SISTEMA DE HÁBITOS === -->
        
        <!-- Receiver para alarmes de tarefas -->
        <receiver 
            android:name=".scheduler.TaskAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.seudominio.vuzixbladeapp.TASK_REMINDER" />
                <action android:name="com.seudominio.vuzixbladeapp.TASK_EXECUTION" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        
        <!-- Receiver para ações de notificação -->
        <receiver 
            android:name=".notifications.TaskNotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.seudominio.vuzixbladeapp.NOTIFICATION_ACTION" />
            </intent-filter>
        </receiver>
        
    </application>

</manifest>